function isSubstring(searchString, subString) {
    // if this is part of the basics, maybe take some time to read up on those extra resources.
    // Presently I would need to google just about every line of syntax. 
    // Coming back to this in a few hours after scanning through codeacademy
}